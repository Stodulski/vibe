# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
e2e/.auth/
e2e-results/
playwright-report/
blob-report/

# Lighthouse CI
lighthouse-results/
.lighthouseci/

# Artillery
load-results.json

# Coverage
coverage/

# CodeGraph local index (machine-specific, per-checkout)
.codegraph/
.atl/

# Local environment. .env.example is the tracked template; the real file is not,
# because VITE_SENTRY_DSN goes in it and everything under VITE_ is compiled into
# the bundle that ships to the browser.
.env
.env.local
.env.*.local

# Local-only material, never published
/.playwright-mcp/
/.git-blame-ignore-revs
/lint-baseline.md
/eslint-disables.md
